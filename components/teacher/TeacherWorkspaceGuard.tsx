"use client";
import{ReactNode,useCallback,useEffect,useState}from"react";import Link from"next/link";import{useParams,useRouter}from"next/navigation";import{supabase}from"@/lib/supabase";
export default function TeacherWorkspaceGuard({children}:{children:ReactNode}){const locale=String(useParams().locale||"my"),router=useRouter(),[loading,setLoading]=useState(true),[allowed,setAllowed]=useState(false);const check=useCallback(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){router.replace(`/${locale}/teacher-login`);return}const[{data},{data:aal}]=await Promise.all([supabase.rpc("can_access_teacher_portal"),supabase.auth.mfa.getAuthenticatorAssuranceLevel()]);if(data&&aal?.currentLevel!=="aal2"){router.replace(`/${locale}/mfa-verify?next=/${locale}/teacher`);return}setAllowed(Boolean(data));setLoading(false)},[locale,router]);useEffect(()=>{// eslint-disable-next-line react-hooks/set-state-in-effect
void check()},[check]);if(loading)return <main className="teacher-access-state">…</main>;if(!allowed){const c=locale==="zh"?{title:"无权访问教师工作台",body:"仅限邮箱已绑定、完成教师认证并已分配课程的教师。作者不能进入后台。",back:"返回我的账户"}:{title:"Instructor workspace unavailable",body:"Only allowlisted, verified teachers assigned to a course may enter. Authors cannot access the backoffice.",back:"Back to my account"};return <main className="auth-page"><section className="auth-card admin-denied"><h1>{c.title}</h1><p>{c.body}</p><Link className="auth-submit auth-submit-link" href={`/${locale}/me`}>{c.back}</Link></section></main>}return <>{children}</>}
