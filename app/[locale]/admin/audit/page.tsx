"use client";
import{useEffect,useState}from"react";import{useParams}from"next/navigation";import AdminGuard from"@/components/admin/AdminGuard";import AdminSidebar from"@/components/admin/AdminSidebar";import{supabase}from"@/lib/supabase";
type Log={id:number;actor_id:string|null;action:string;target_table:string;target_id:string|null;created_at:string};export default function Page(){return <AdminGuard><Body/></AdminGuard>}function Body(){const locale=String(useParams().locale||"en");const[items,setItems]=useState<Log[]>([]);const[error,setError]=useState("");const title=locale==="zh"?"管理员操作日志":locale==="my"?"အက်မင် လုပ်ဆောင်ချက်မှတ်တမ်း":"Admin audit log";useEffect(()=>{supabase.from("admin_audit_logs").select("id,actor_id,action,target_table,target_id,created_at").order("created_at",{ascending:false}).limit(200).then(({data,error})=>{setItems((data||[])as Log[]);setError(error?.message||"")})},[]);return <div className="adminShell"><AdminSidebar/><main className="adminContent"><h1>{title}</h1>{error&&<div className="feedCard">{error}</div>}<div className="audit-list">{items.map(x=><article key={x.id}><span className={`audit-action is-${x.action.toLowerCase()}`}>{x.action}</span><strong>{x.target_table}{x.target_id?` #${x.target_id}`:""}</strong><code>{x.actor_id||"system"}</code><time>{new Date(x.created_at).toLocaleString()}</time></article>)}</div></main></div>}
